sudo: required

language: node_js

services:
  - docker

install:
- npm install

script:
- npm run build
- npm run test
- npm run build-docker

after_success:
- docker login -u "$DOCKER_USERNAME" -p "$DOCKER_PASSWORD"
- docker push champgm/home-dashboard;